<div class="main">
  <div class="inner">
    <div class="prototype__wrapper">
      <p class="prototype__hedding">
        <%# プロトタイプ名を表示します %>
        <%= @prototype.title %>
      </p>
      <%# 投稿者名を表示し、ユーザー詳細ページへのリンクとします。 %>
      <%= link_to "by #{@prototype.user.username}", user_path(@prototype.user), class: :prototype__user %>
      <%# ログインしているユーザーが投稿者本人である場合にのみ、「編集」と「削除」ボタンを表示します %>
      <% if user_signed_in? && current_user.id == @prototype.user_id %>
       <div class="prototype__manage">
         <%= link_to "編集する", edit_prototype_path(@prototype), class: :prototype__btn %>
         <%= link_to "削除する", prototype_path(@prototype), data: { turbo_method: :delete }, class: :prototype__btn %>
       </div>
      <% end %>
      <%# プロトタイプの画像を表示します。画像が添付されている場合のみ表示 %>
      <div class="prototype__image">
        <%= image_tag @prototype.image %>
      </div>
      <div class="prototype__body">
        <div class="prototype__detail">
          <p class="detail__title">キャッチコピー</p>
          <p class="detail__message">
            <%# プロトタイプのキャッチコピーを表示します %>
            <%= @prototype.catch_copy %>
          </p>
        </div>
        <div class="prototype__detail">
          <p class="detail__title">コンセプト</p>
          <p class="detail__message">
            <%# プロトタイプのコンセプトを表示します %>
            <%= @prototype.concept %>
          </p>
        </div>
      </div>

      <%# コメント投稿フォームとコメント一覧を一つのブロックにまとめる %>
      <div class="prototype__comments">
        <% if user_signed_in? %>
          <%# コメント投稿フォーム %>
          <%= form_with model: [@prototype, @comment], local: true do |f| %>
            <div class="field">
              <%= f.label :text, "コメント" %><br />
              <%= f.text_field :text, id:"comment_content" %>
            </div>
            <div class="actions">
              <%= f.submit "送信する", class: :form__btn  %>
            </div>
            <%# エラーメッセージの表示 %>
            <% if @comment.errors.any? %>
              <ul class="error-list">
                <% @comment.errors.full_messages.each do |message| %>
                  <li class="error-message"><%= message %></li>
                <% end %>
              </ul>
            <% end %>
          <% end %>
        <% end %>

        <ul class="comments_lists">
          <%# 投稿に紐づくコメントを全て表示します %>
          <% @comments.each do |comment| %>
            <li class="comments_list">
                <%= comment.text %>
                <%# <span class="comments_list__user"> %>
                  <%= link_to "（#{comment.user.username}）", user_path(comment.user), class: :comment_user %>
            </li>
          <% end %>
        </ul>
      </div>

    </div>
  </div>
</div>